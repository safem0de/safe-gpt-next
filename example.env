# ============================================
# MongoDB Configuration
# ============================================
MONGODB_URI=mongodb://r***t:e****e@localhost:27017/Safem0de-gpt?authSource=admin
MONGODB_COLL=chats

# ============================================
# AI Model Configuration (Google Gemini)
# ============================================
GOOGLE_GENERATIVE_AI_API_KEY=AIza**********************nXcw
AI_MODEL=gemini-2.0-flash

# ============================================
# Keycloak Configuration
# ============================================
# Keycloak Client ID (ได้จากการสร้าง Client ใน Keycloak)
KEYCLOAK_CLIENT_ID=safem0de-gpt-client

# Keycloak Client Secret (ได้จาก Keycloak Admin Console > Clients > safem0de-gpt-client > Credentials)
# TODO: แทนที่ด้วยค่าจริงจาก Keycloak
KEYCLOAK_CLIENT_SECRET=YOUR_CLIENT_SECRET_HERE

# Keycloak Issuer URL
# Format: http://{keycloak-host}:{port}/realms/{realm-name}
KEYCLOAK_ISSUER=http://localhost:8080/realms/safem0de-gpt

# ============================================
# NextAuth Configuration
# ============================================
# NextAuth URL (URL ของ Next.js application)
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret (สร้างด้วยคำสั่ง: openssl rand -base64 32)
# TODO: แทนที่ด้วย secret ที่สร้างใหม่
NEXTAUTH_SECRET=YOUR_NEXTAUTH_SECRET_HERE_PLEASE_GENERATE_NEW_ONE

# Cookie Prefix (ป้องกัน session ชนกันเมื่อรันหลายระบบบน localhost)
# สำหรับ dev: safem0de-gpt-dev
# สำหรับ staging: safem0de-gpt-staging
# สำหรับ production: safem0de-gpt
NEXTAUTH_COOKIE_PREFIX=safem0de-gpt-dev

# ============================================
# Public Environment Variables
# ============================================
# Keycloak Logout URL (สำหรับ logout จาก Keycloak และ redirect กลับมาที่ app)
NEXT_PUBLIC_KEYCLOAK_LOGOUT_URL=http://localhost:8080/realms/safem0de-gpt/protocol/openid-connect/logout?redirect_uri=http://localhost:3000

# ============================================
# 📝 TODO - ต้องทำก่อนใช้งาน:
# ============================================
# 1. สร้าง Realm ใน Keycloak ชื่อ "safem0de-gpt"
# 2. สร้าง Client ชื่อ "safem0de-gpt-client" แล้วคัดลอก Client Secret
# 3. สร้าง NextAuth Secret ด้วยคำสั่ง: openssl rand -base64 32
# 4. แทนที่ค่า TODO ด้านบน
# 5. Restart Next.js dev server
