# ============================================
# MongoDB Configuration
# ============================================
MONGODB_URI=mongodb://r***t:e****e@localhost:27017/Safem0de-gpt?authSource=admin
MONGODB_COLL=chats

# ============================================
# AI Model Configuration (Google Gemini)
# ============================================
GOOGLE_GENERATIVE_AI_API_KEY=AIza**********************nXcw
AI_MODEL=gemini-2.0-flash

# ============================================
# RAG Backend API
# ============================================
# Base URL that Next.js will call for retrieval (use host the container can reach)
RAG_API_BASE_URL=http://localhost:8000
RAG_USER=YOUR_RAG_USERNAME
RAG_PASS=YOUR_RAG_PASSWORD
RAG_AUTH_URL=http://localhost:8000/auth/login

# ============================================
# Keycloak Configuration
# ============================================
KEYCLOAK_CLIENT_ID=safem0de-gpt-client
KEYCLOAK_CLIENT_SECRET=YOUR_CLIENT_SECRET_HERE
KEYCLOAK_ISSUER=http://localhost:8080/realms/safem0de-gpt

# ============================================
# NextAuth Configuration
# ============================================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=YOUR_NEXTAUTH_SECRET_HERE_PLEASE_GENERATE_NEW_ONE
NEXTAUTH_COOKIE_PREFIX=safem0de-gpt-dev

# ============================================
# Public Environment Variables
# ============================================
NEXT_PUBLIC_KEYCLOAK_LOGOUT_URL=http://localhost:8080/realms/safem0de-gpt/protocol/openid-connect/logout?redirect_uri=http://localhost:3000

# ============================================
# TODO before use
# 1. Create realm safem0de-gpt in Keycloak.
# 2. Create client safem0de-gpt-client and paste its Client Secret.
# 3. Generate a new NEXTAUTH_SECRET: openssl rand -base64 32 (or scripts/generate-secret.sh).
# 4. Set KEYCLOAK_ISSUER / NEXTAUTH_URL to the correct host for your env.
# 5. Restart the server after updating env values.

RAG_API_BASE_URL=http://localhost:8000
RAG_AUTH_URL=http://localhost:8000/auth/login
RAG_USER=demo
RAG_PASS=change-me